# 🔍 Руководство по Отладке Бота

## 🧪 Первый шаг: Проверка конфигурации

### Запустите диагностический скрипт:

```bash
python check.py
```

Этот скрипт проверит:
- ✅ Версию Python (нужна 3.11+)
- ✅ Наличие .env файла
- ✅ Корректность BOT_TOKEN и ADMIN_ID
- ✅ Структуру проекта
- ✅ Установленные модули

---

## 📄 Если есть ошибки:

### Ошибка: "No module named 'aiogram'"
```bash
# Переустановите зависимости
pip install --upgrade -r requirements.txt
```

### Ошибка: "BOT_TOKEN is not properly configured"
```bash
# Проверьте .env файл:
cat .env | grep BOT_TOKEN

# Должно быть:
# BOT_TOKEN=8382050069:AAE5mkJao3hbRW29abMOPnoUl3RC0T29P3E

# Отредактируйте .env и убедитесь что нет пробелов и текста
```

### Ошибка: "ADMIN_ID must be a number"
```bash
# В .env должно быть:
ADMIN_ID=123456789

# НЕ ДОЛЖНО БЫТЬ:
ADMIN_ID=YOUR_ADMIN_USER_ID  # ❌ ТЕКСТ
```

### Ошибка: "Database connection failed"
```bash
# SQLite база создается автоматически
# Если ошибка все еще есть, удалите старую базу:
rm bearsmoney.db
python main.py
```

---

## 📄 Если бот не запускается:

### 1. Проверьте логи:
```bash
# Linux/Mac
tail -f logs/bot.log

# Windows (PowerShell)
Get-Content logs\bot.log -Wait
```

### 2. Запустите с полным выводом ошибок:
```bash
# Удалите старый лог
rm logs/bot.log

# Запустите бота
python main.py

# Скопируйте весь вывод и ошибку из консоли
```

### 3. Проверьте что виртуальное окружение активировано:

**Windows (PowerShell):**
```powershell
# Должно быть (venv) в начале строки
venv\Scripts\activate

# Проверьте что активировано
python --version  # Должна быть 3.11+
```

**Linux/Mac:**
```bash
# Должно быть (venv) в начале строки
source venv/bin/activate

# Проверьте
python3 --version
```

---

## 🐖 Если бот запустился но не отвечает:

### 1. Проверьте что бот правильно запущен:
```
2026-01-15 21:54:16 - __main__ - INFO - 🐻 Starting BearsMoney Bot...
2026-01-15 21:54:16 - app.database.db - INFO - ✅ Database initialized
2026-01-15 21:54:16 - app.bot - INFO - ✅ All handlers registered successfully
2026-01-15 21:54:16 - __main__ - INFO - 🚀 Bot is running! Send /start to test
```

### 2. Найдите вашего бота в Telegram и отправьте `/start`

### 3. Если нет ответа, проверьте логи:
```bash
tail -f logs/bot.log
```

---

## 📊 Список возможных ошибок и решений:

| Ошибка | Причина | Решение |
|--------|---------|----------|
| `ValueError: BOT_TOKEN not set` | Отсутствует токен в .env | Добавьте BOT_TOKEN в .env |
| `invalid literal for int()` | ADMIN_ID не число | Измените на число в .env |
| `ModuleNotFoundError: aiogram` | Не установлены зависимости | `pip install -r requirements.txt` |
| `asyncio.TimeoutError` | Интернет проблема | Проверьте интернет |
| `sqlite3.OperationalError` | Проблема с БД | Удалите bearsmoney.db и заново запустите |
| `Connection refused (Redis)` | Redis не нужен для локальной разработки | Игнорируйте эту ошибку |

---

## 📻 Как получить ваш ADMIN_ID:

1. Откройте Telegram
2. Найдите бота **@userinfobot**
3. Отправьте ему любое сообщение
4. Он ответит с вашим User ID
5. Скопируйте это число (без текста) в ADMIN_ID в .env

---

## 👍 Если ничего не помогло:

1. Скопируйте **ВЕСЬ текст ошибки** из консоли
2. Проверьте что .env файл правильно заполнен
3. Удалите и заново создайте виртуальное окружение:
   ```bash
   rm -rf venv  # или rmdir venv\s /q (Windows)
   python -m venv venv
   source venv/bin/activate  # или venv\Scripts\activate (Windows)
   pip install -r requirements.txt
   python main.py
   ```

---

## 💡 Советы:

- Всегда проверьте логи в `logs/bot.log`
- Используйте `python check.py` перед запуском
- Убедитесь что используете Python 3.11+
- Виртуальное окружение должно быть всегда активировано
- Не меняйте структуру папок проекта

---

**Удачи в разработке! 🚀**
